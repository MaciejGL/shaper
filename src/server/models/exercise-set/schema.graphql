type ExerciseSet {
  id: ID!
  order: Int!
  reps: Int
  minReps: Int
  maxReps: Int
  weight: Float
  rpe: Int

  logs: [ExerciseSetLog!]!
  exerciseId: ID!

  createdAt: String!
  updatedAt: String!
}
