type ExerciseSet {
  id: ID!
  order: Int!
  reps: Int
  minReps: Int
  maxReps: Int
  weight: Float
  rpe: Int

  completedAt: String
  log: ExerciseSetLog
  exerciseId: ID!

  createdAt: String!
  updatedAt: String!
}
