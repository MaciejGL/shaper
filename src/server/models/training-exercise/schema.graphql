type TrainingExercise {
  id: ID!
  name: String!
  restSeconds: Int
  tempo: String
  instructions: String
  order: Int!

  sets: [ExerciseSet!]!
  logs: [ExerciseLog!]!

  # baseExercise: BaseExercise
  dayId: ID!

  createdAt: String!
  updatedAt: String!
}
