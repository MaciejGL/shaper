type TrainingExercise {
  id: ID!
  name: String!
  restSeconds: Int
  tempo: String
  warmupSets: Int
  instructions: String
  order: Int!

  sets: [ExerciseSet!]!
  logs: [ExerciseLog!]!

  # baseExercise: BaseExercise
  dayId: ID!

  createdAt: String!
  updatedAt: String!
}
