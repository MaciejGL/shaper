type TrainingExercise {
  id: ID!
  name: String!

  restSeconds: Int
  tempo: String
  instructions: String
  order: Int!

  sets: [ExerciseSet!]!

  # baseExercise: BaseExercise
  dayId: ID!
  logs: [ExerciseLog!]!

  createdAt: String!
  updatedAt: String!
}
