type TrainingWeek {
  id: ID!
  weekNumber: Int!
  completedAt: String
  name: String!
  description: String
  trainingPlanId: ID!
  days: [TrainingDay!]!

  scheduledAt: String

  createdAt: String!
  updatedAt: String!
}
