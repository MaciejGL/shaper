mutation AdminGenerateAiWorkout($input: GenerateAiWorkoutInput!) {
  generateAiWorkout(input: $input) {
    exercises {
      exercise {
        id
        name
        equipment
        muscleGroups {
          name
        }
      }
      sets {
        minReps
        maxReps
        rpe
      }
      order
    }
    totalDuration
  }
}
